spring:
  datasource:
    url: jdbc:mysql://localhost:34002/inventory
    username: inventory
    password: user123

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop

server:
  port: 8082
